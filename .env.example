# Session signing (required in production). Generate: openssl rand -hex 32
MCP_SESSION_SECRET=

# Coinbase OAuth2 – for "Sign in with Coinbase" (users sign in on Coinbase's site).
# Create an app at https://www.coinbase.com/settings/api and set redirect URI to:
#   https://YOUR-RENDER-URL.onrender.com/auth/callback
COINBASE_CLIENT_ID=
COINBASE_CLIENT_SECRET=

# Optional: host/port when running `npm run start` locally
# MCP_HOST_PORT=3100
# MCP_HOST_HOST=127.0.0.1
